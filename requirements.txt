# HTTP 请求
httpx==0.27.0

# 环境变量
python-dotenv==1.0.1

# 数据库 (Turso 云端)
libsql-experimental==0.0.49; sys_platform == "linux"

# 加密（企微回调验证）
pycryptodome==3.20.0

# 数据处理（本地/Actions 用）
pandas==2.2.0

# 定时任务（本地开发用）
schedule==1.2.1
